# 1. Given a list of integers, return a list where each integer is multiplied by 2.


def doubling(){
    map(*(2))
}

#set double = doubling()




#This should parse to: functionCall.  Instead it is parsing to functionCall, Grouping
doubling()(list(1, 2, 3))

